# vitepress-slidev-plugins

这是插件集合项目的调试与文档入口。

## 本地调试

```bash
pnpm install
pnpm docs:dev
```

## 目录说明

- `packages/`：插件子包
- `docs/`：VitePress 文档与调试站点

## 下一步

- 在 `packages/` 继续新增插件子包
- 在 `docs/` 中增加对应插件的示例页面
